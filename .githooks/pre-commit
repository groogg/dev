#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
    echo "Error: gitleaks is required to commit. Run 'dev install' to install it." >&2
    exit 1
fi

exec gitleaks git --pre-commit --staged --redact --no-banner
