<div align="center">

<img src="./alien.svg" width="100" />


</div>

```bash
git clone git@github.com:groogg/dev.git ~/dev && make -C ~/dev setup-mac
```

```bash
# VM / headless Linux
git clone git@github.com:groogg/dev.git ~/dev && make -C ~/dev setup-vm
```

```bash
# VM / headless Linux (no SSH key)
git clone https://github.com/groogg/dev.git ~/dev && make -C ~/dev setup-vm
```
